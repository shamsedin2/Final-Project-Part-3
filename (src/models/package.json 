{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
{
  "scripts": {
    "test": "jest"
  }
}
{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  }
}